<form method="post" action="{{ path('app_releves_delete', {'id': resultat.releve.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ resultat.releve.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
